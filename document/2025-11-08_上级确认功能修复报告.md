# 上级确认功能修复报告（2025-11-08）

## 🐛 问题描述

用户发现上级确认功能存在严重问题：

1. **状态显示错误**：确认后状态变成"待完成"，而不是"已确认"
2. **上级人员丢失**：历史记录中上级人员显示为空，没有记录是谁点的确认

---

## 🔍 问题根源分析

### 根源1：未传递确认人姓名

**代码位置**：`registry/hooks.py` 第415-416行

**问题**：
```python
# 更新状态为confirmed
mark_confirmed(db_path, wal, key, now)  # ❌ 没有传递user_name
```

**影响**：
- `mark_confirmed`函数有`confirmed_by`参数（默认None）
- 但调用时没有传递`user_name`
- 导致数据库中`confirmed_by`字段永远是NULL

---

### 根源2：历史查询UI使用错误字段

**代码位置**：`registry/history_ui.py` 第320行

**问题**：
```python
superior_name = task.get('superior_name') or '-'  # ❌ 数据库中没有这个字段
```

**影响**：
- 数据库表中没有`superior_name`字段
- 实际应该使用`confirmed_by`字段
- 导致历史记录中上级人员永远显示"-"

---

### 根源3：已确认任务被重置状态

**代码位置**：`registry/service.py` 第617-666行

**问题**：
- `batch_upsert_tasks`在每次处理时被调用
- 状态继承逻辑没有特殊处理已确认任务
- 如果完成列被删除或接口时间变化，会重置为"待完成"

**影响**：
- 已确认的任务再次出现在Excel中时
- 可能被重置为`status='open'`, `display_status='待完成'`
- 历史记录中显示"待完成"而不是"已确认"

---

### 根源4：状态映射不完整

**代码位置**：`registry/history_ui.py` 第22-29行

**问题**：
- `STATUS_DISPLAY_MAP`缺少`'open'`和`'completed'`状态
- 只有旧的兼容状态

**影响**：
- 如果`display_status`为空，从`status`字段获取
- 但`status='open'`在映射表中不存在
- 回退显示原始状态值

---

## ✅ 修复方案

### 修复1：传递确认人姓名

**文件**：`registry/hooks.py` 第415-416行

**修复前**：
```python
mark_confirmed(db_path, wal, key, now)
```

**修复后**：
```python
mark_confirmed(db_path, wal, key, now, confirmed_by=user_name)
```

**效果**：
- ✅ 数据库`confirmed_by`字段正确记录确认人姓名
- ✅ 历史记录中可以查看是谁确认的

---

### 修复2：修正历史查询UI字段

**文件**：`registry/history_ui.py` 第320、370行

**修复前**：
```python
superior_name = task.get('superior_name') or '-'
```

**修复后**：
```python
# 【修复】上级人员：使用confirmed_by字段
superior_name = task.get('confirmed_by') or '-'
```

**影响位置**：
- 表格显示（第320行）
- 导出Excel（第370行）

**效果**：
- ✅ 历史记录正确显示确认人姓名
- ✅ 导出Excel包含确认人信息

---

### 修复3：保护已确认任务

**文件**：`registry/service.py` 第619-630行

**修复**：在状态继承逻辑前增加特殊判断

**新增逻辑**：
```python
# 【新增】如果旧任务已确认，跳过所有逻辑，保持确认状态
if old_task['status'] == Status.CONFIRMED and old_task['confirmed_at']:
    # 已确认的任务不再参与状态流转
    fields['status'] = Status.CONFIRMED
    fields['display_status'] = None  # 保持不显示
    fields['confirmed_at'] = old_task['confirmed_at']
    fields['confirmed_by'] = old_task['confirmed_by']
    fields['completed_at'] = old_task['completed_at']
    if old_task['assigned_by']:
        fields['assigned_by'] = old_task['assigned_by']
        fields['assigned_at'] = old_task['assigned_at']
        fields['responsible_person'] = old_task['responsible_person']
else:
    # 未确认的任务，正常处理（原有逻辑）
    ...
```

**效果**：
- ✅ 已确认任务不会被重置
- ✅ confirmed_by和confirmed_at永久保留
- ✅ 历史记录中状态永远是"已确认"

---

### 修复4：完善batch_upsert_tasks的INSERT语句

**文件**：`registry/service.py` 第675-732行

**修复内容**：
1. 从fields获取confirmed相关字段
2. INSERT语句包含confirmed_at和confirmed_by
3. ON CONFLICT逻辑保留这些字段

**修复后的INSERT**：
```sql
INSERT INTO tasks (
    ..., confirmed_by, completed_at, confirmed_at
)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
ON CONFLICT(id) DO UPDATE SET
    ...
    confirmed_by = COALESCE(excluded.confirmed_by, confirmed_by),
    completed_at = COALESCE(excluded.completed_at, completed_at),
    confirmed_at = COALESCE(excluded.confirmed_at, confirmed_at)
```

**效果**：
- ✅ 确认信息在INSERT时正确传递
- ✅ ON CONFLICT时保留确认信息

---

### 修复5：完善状态映射

**文件**：`registry/history_ui.py` 第22-32行

**修复前**：
```python
STATUS_DISPLAY_MAP = {
    'pending': '待完成',
    'assigned': '待指派人审查',
    'in_review': '待审查',
    'confirmed': '已确认',
    'archived': '已归档',
}
```

**修复后**：
```python
# 状态映射（与数据库中的status字段对应）
STATUS_DISPLAY_MAP = {
    'open': '待完成',           # Status.OPEN
    'completed': '已完成',      # Status.COMPLETED
    'confirmed': '已确认',      # Status.CONFIRMED
    'archived': '已归档',       # Status.ARCHIVED
    # 以下为旧状态，保留兼容
    'pending': '待完成',
    'assigned': '待指派人审查',
    'in_review': '待审查',
}
```

**效果**：
- ✅ 完整映射所有数据库status值
- ✅ 历史记录正确显示状态

---

## 📊 修复统计

| 修复项 | 文件 | 修改位置 | 修改行数 |
|--------|------|---------|----------|
| 传递确认人姓名 | registry/hooks.py | 第416行 | 1行 |
| 修正UI字段 | registry/history_ui.py | 第320、370行 | 2行 |
| 保护已确认任务 | registry/service.py | 第619-630行 | 新增14行 |
| 完善INSERT语句 | registry/service.py | 第675-732行 | 修改60行 |
| 完善状态映射 | registry/history_ui.py | 第22-32行 | 修改10行 |
| **总计** | **3个文件** | - | **约87行** |

---

## 🔄 完整的上级确认流程

### 修复后的流程

1. **上级点击"已完成"勾选框**
   - 触发`on_confirmed_by_superior(user_name="王五")`
   - 调用`mark_confirmed(..., confirmed_by="王五")`
   
2. **更新数据库**
   ```sql
   UPDATE tasks SET 
       status = 'confirmed',
       confirmed_at = '2025-11-08 15:30:00',
       display_status = NULL,
       confirmed_by = '王五'
   WHERE id = ?
   ```

3. **主窗口显示**
   - `display_status = NULL` → 不显示该任务
   - 已确认任务被过滤掉

4. **再次处理时**
   - `batch_upsert_tasks`检测到`status='confirmed'`
   - 跳过所有状态流转逻辑
   - 保持confirmed状态

5. **历史查询**
   - `display_status = NULL` → 查询`status='confirmed'`
   - `STATUS_DISPLAY_MAP['confirmed']` → "已确认"
   - `confirmed_by` → "王五"
   - 正确显示："状态=已确认，上级人员=王五"

---

## 🧪 测试验证

### 测试1：上级确认操作

**步骤**：
1. 以"所领导"角色登录
2. 查看一个"待审查"的任务
3. 勾选"已完成"
4. 查看历史记录

**预期结果**：
- ✅ 主窗口不再显示该任务
- ✅ 历史记录状态显示"已确认"
- ✅ 历史记录上级人员显示当前用户姓名（如"王五"）
- ✅ 确认时间正确记录

---

### 测试2：再次处理不重置

**步骤**：
1. 确认一个任务
2. 再次点击"开始处理"
3. 查看历史记录

**预期结果**：
- ✅ 任务仍不显示在主窗口
- ✅ 历史记录状态仍为"已确认"
- ✅ 确认人和确认时间未变

---

### 测试3：导出Excel

**步骤**：
1. 查询已确认任务的历史记录
2. 点击"导出Excel"

**预期结果**：
- ✅ Excel包含"上级人员"列
- ✅ 显示确认人姓名
- ✅ 状态显示"已确认"

---

## 📝 数据库表结构说明

### tasks表相关字段

| 字段 | 类型 | 说明 | 写入时机 |
|------|------|------|---------|
| status | TEXT | 内部状态（open/completed/confirmed/archived） | 创建/完成/确认/归档 |
| display_status | TEXT | 显示状态（待完成/待审查/NULL） | 创建/指派/写回文单号/确认 |
| completed_at | TEXT | 完成时间 | 写回文单号时 |
| confirmed_at | TEXT | 确认时间 | 上级确认时 |
| confirmed_by | TEXT | 确认人姓名 | 上级确认时 |
| assigned_by | TEXT | 指派人姓名 | 指派任务时 |
| responsible_person | TEXT | 责任人（源文件中的） | 扫描Excel时 |

### 状态流转图

```
open (待完成)
  ↓ 写回文单号
completed (待审查, display_status='待审查')
  ↓ 上级确认
confirmed (display_status=NULL, confirmed_by='王五')
  → 不再参与状态流转
  → 历史查询显示"已确认"
```

---

## ✅ 总结

### 修复前的问题

1. ❌ 上级确认后，确认人丢失（confirmed_by=NULL）
2. ❌ 历史记录显示上级人员为空
3. ❌ 历史记录状态显示"待完成"而不是"已确认"
4. ❌ 再次处理时可能重置已确认任务

### 修复后的效果

1. ✅ 确认人正确记录到数据库
2. ✅ 历史记录正确显示确认人姓名
3. ✅ 历史记录状态显示"已确认"
4. ✅ 已确认任务永久保持确认状态
5. ✅ 导出Excel包含完整确认信息

---

## 🔧 相关函数说明

### mark_confirmed()
```python
def mark_confirmed(db_path, wal, key, now, confirmed_by=None):
    """
    标记任务为已确认
    
    - 设置status='confirmed'
    - 设置confirmed_at=当前时间
    - 设置confirmed_by=确认人姓名
    - 设置display_status=NULL（不再显示）
    """
```

### on_confirmed_by_superior()
```python
def on_confirmed_by_superior(file_type, file_path, row_index, 
                             user_name, project_id, interface_id, now):
    """
    上级确认钩子
    
    - 构造任务key
    - 调用mark_confirmed(confirmed_by=user_name)  # 传递确认人
    - 写入confirmed事件
    """
```

### batch_upsert_tasks() - 已确认任务保护
```python
if old_task['status'] == Status.CONFIRMED and old_task['confirmed_at']:
    # 已确认的任务不再参与状态流转
    fields['status'] = Status.CONFIRMED
    fields['confirmed_by'] = old_task['confirmed_by']  # 保留确认人
    fields['confirmed_at'] = old_task['confirmed_at']  # 保留确认时间
```

---

**修复完成时间**：2025-11-08  
**Token使用**：约11.2万/100万（11.2%）  
**状态**：✅ 所有问题已修复  
**无linter错误**：✅ 通过检查

---

## ⚠️ 重要更新

**本报告中"历史记录中状态永远是'已确认'"的描述有误**

实际业务场景中，已确认的任务可能会重新打开（完成列被清空），此时应该重置状态。

**详细修复**：请参见 `document/2025-11-08_已确认任务重新打开逻辑修复.md`

**修复后的正确逻辑**：
- ✅ 已确认任务如果完成列被清空 → 强制重置为"待完成"
- ✅ 已确认任务如果完成列仍有值 → 保持"已确认"状态

