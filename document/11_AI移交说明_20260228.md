# AI移交说明（截至 2026-02-28）

本文用于把当前分支已有工作完整移交给下一位 AI。  
目标是让接手方无需二次追溯即可理解：**业务目标、实现现状、验证结论、遗留问题、下一步执行路径**。

---

## 1. 仓库与上下文快照

- 仓库路径：`e:/program/PythonProject3`
- 当前分支：`SQL路径起点`
- 当前提交：`22362af`
- 当前工作区：本轮生成说明时 `git status --short` 无输出（工作区干净）
- 历史完整对话记录文件：  
  `C:\Users\hasee\.cursor\projects\e-program-PythonProject3/agent-transcripts/16c51da5-95d9-4a6a-97b6-35c46f220f6f.txt`

---

## 2. 用户核心目标（必须遵守）

### 2.1 总目标

把“待处理文件”从 Excel 驱动逐步切换到数据库驱动（SQL Server），先做离线 `CIMS-sql` 对照验证，再落地程序逻辑。

### 2.2 文件2（当前重点）硬约束

1. 仅考虑 `1818` 项目。
2. 仅按 `document/2_模块功能说明.md` 第 69 行业务列验证：`A/F/I/M/N/AB`（并明确接口号口径）。
3. 需要多表联动，不接受单表拍脑袋映射。
4. 匹配率要求尽量 100%，至少 >90%，并持续迭代。
5. 明确“接口号”定义：
   - Excel `A` 列是信息单号（`INTINTERFACEDOC.ITEM_NUMBER`）
   - 程序 `interface_id` 应是 Excel `R` 列（IDI编号，经桥表映射 `IDIACP1000.ITEM_NUMBER`）
6. `I/M/N` 三列需重点深挖：
   - `I`：科室语义匹配（代码或全路径）
   - `M`：寻找潜在日期计算关系（如 `某列 + X 天`）
   - `N`：空对空算符合，非空允许合理日期容差
7. 责任人（文件2）必须找“完成人口径”，明确不是设总列。

---

## 3. 已完成工作总览

---

### 3.1 文件1（内部需打开接口）数据库驱动改造：已完成并已实机通过

用户已确认“待处理文件1实机测试通过”。

#### 关键实现（代码侧）

- 新增模块：`core/sql/file1_db_source.py`
  - `db://file1/{project_id}` 虚拟源机制
  - 复用 `sql_explorer` 配置建连
  - 查询 `IDIACP1000` 当前版本数据（`IS_CURRENT` + `MODIFIED_ON`排序）
  - `USER` 关联责任人解析
  - 导出收敛为 4 列：`接口号/接口日期/责任人/所属科室`

- `core/main.py`
  - `process_target_file()` 支持文件1虚拟源走 SQL，不再读 Excel
  - 文件1数据库模式导出走精简列，不依赖模板拷贝
  - 修复空 DataFrame 早退逻辑（空文件时无条件 return）

- `base.py`
  - UI 增加文件1数据库连接测试入口
  - 刷新逻辑显示“文件1数据库连接状态”
  - 文件1数据库模式下：
    - 不走 Excel 文件识别
    - 不走 Registry 写入
    - 不走“已完成勾选/待确认”过滤
    - 不参与指派
  - 增加数据库诊断报告生成功能（txt）

- `.gitignore`
  - 增加 `.tmp_*`、`example/CIMS-sql/`、`CIMS-sql/`、若干本地产物忽略规则

#### 文件1当前状态结论

- 功能已落地到主程序路径。
- 用户反馈“实机测试通过”，当前不是阻塞点。

---

### 3.2 文件2 SQL 对照与验证（离线）：已完成多轮，结论已写入文档

主文档：`document/7_待处理文件SQL对照表.md`

#### 关键结论（已写入）

1. 接口号口径澄清（关键）  
   - `A` 列 -> `INTINTERFACEDOC.ITEM_NUMBER`（信息单编号）  
   - 程序 `interface_id` -> `R` 列（IDI 编号），通过 `INTINTERFACEDOCIDIACP1000` 关联 `IDIACP1000.ITEM_NUMBER`

2. 责任人（完成人）主映射  
   - `INTINTERFACEDOC.MODIFIED_BY_ID -> USER`（已完成接口覆盖 `100%`，`4377/4377`）
   - `RESP_SHEZONG` 仅作“责任角色（设总口径）”，不作完成人主口径

3. 1818 全量逐行复测核心数据（文档 4.5）
   - Excel `R` 列数据库存在性：`100%`（`15776/15776`）
   - `R` 联动到 `INTINTERFACEDOC`：`99.9937%`（`15775/15776`）
   - 业务列逐字一致率（文本直比）：
     - `A/F/R`：`99.9937%`
     - `AB`：`96.5137%`
     - `N`：`79.5766%`
     - `I`：`11.2513%`
     - `M`：`0.2028%`

4. 业务规则一致率（文档 4.4）
   - `P1/P2/P3/P4` 综合逻辑一致率：`99.9493%`
   - 剩余 8 条可通过特例覆盖兜底（已有样例文件）

---

### 3.3 I/M/N 深挖分析：已完成离线探测，尚未写入运行时代码

#### I 列（收文部门）

- 核心候选：`INT.RECEIVE_DEPT`
- 结论：逐字匹配不可靠，语义/代码匹配明显更合理。
- 已结合 `config.json` 的 4 所映射做专项核验（见 4.7）

#### M 列（回文期限）

- 已做日期位移扫描（shift-day）。
- 观察到存在一定“隐式日期关系”，但非单一全量公式可 100% 覆盖。
- 候选关系在不同子集（传递/回复）表现不一（如 `START_DATE + 15`、`ACTUAL_OPEN_DATE + 14`）。

#### N 列（回文日期）

- `INT.ANSWER_DATE` 是最佳主候选。
- “空对空”计为符合，加入 ±1/±3 天容差后匹配率显著上升。

---

### 3.4 4所 I 列专项与冲突样本独立文件：已完成

#### 已新增文档段落

- `document/7_待处理文件SQL对照表.md` 增加 `4.7 文件2 I列（4所）专项结论 + 冲突样本独立清单`

#### 4所匹配率（`INT.RECEIVE_DEPT`）

- 建筑结构所：`95.5902%`（1474 行）
- 核工程研究设计所：`98.1833%`（1211 行）
- 电力工程研究设计所：`100.0000%`（111 行）
- 电气自动化所：`99.3776%`（1928 行）

#### 冲突样本文件（独立）

- 文件：`document/file2_i_conflict_samples.json`
- 全量冲突：`24` 条（建筑结构所 `21`，电气自动化所 `3`）
- 原因统计：
  - `21`：库内已变更为其他所/其他代码（需业务确认）
  - `2`：SQL收文部门未出现该所代码
  - `1`：同 `R` 链路可命中（疑似桥表关联行选择差异）

#### “接口版本不一致”专项检查结论

- 在该 24 条中：
  - `same_item_other_rev_has_excel_code = 0/24`
  - `same_item_same_rev_has_excel_code = 0/24`
- 结论：当前冲突**未发现明确版次不一致证据**；主因更偏口径差异与库内科室变更。

---

## 4. 关键脚本与产物清单（供下一位AI复用）

### 4.1 临时脚本（分析逻辑）

- `.tmp_file2_1818_full_validation.py`  
  文件2全量联动验证主脚本（A/F/I/M/N/AB + R + 责任人扫描）

- `.tmp_file2_imn_deepdive.py`  
  I/M/N 深挖（含日期位移、容差、语义匹配）

- `.tmp_file2_i_4profiles_check.py`  
  4所专项 I 列匹配率检查

- `.tmp_file2_i_conflict_reason_analyze.py`  
  生成独立冲突样本文件并追加“原因诊断/版本检查”字段

> 注：`.gitignore` 当前包含 `.tmp_*`，这些脚本默认不会入库。

### 4.2 主要结果文件（文档/JSON）

- `document/7_待处理文件SQL对照表.md`（主结论）
- `document/file2_1818_full_validation_latest.json`
- `document/file2_imn_deepdive_latest.json`
- `document/file2_i_4profiles_check_latest.json`
- `document/file2_i_conflict_samples.json`
- `document/file2_final_mismatch_8rows.json`
- `document/file2_1818_special_overrides.json`

---

## 5. 当前“已完成 / 未完成”明确边界

### 5.1 已完成

1. 文件1数据库驱动改造（UI/处理/导出/只读约束）已落地，用户实机通过。  
2. 文件2（1818）离线多表联动分析与文档化已完成。  
3. 文件2接口号口径争议已澄清（A vs R）。  
4. 文件2责任人“完成人口径”已定位并给出 100% 可解析映射。  
5. 4所 I列专项结论 + 独立冲突样本文件 + 冲突原因诊断已完成。  
6. 对冲突做了“是否版次不一致”专项检查，结论已给出。  

### 5.2 未完成（下一位AI优先处理）

1. **把文件2 I/M/N 的最优规则写入主程序运行时代码**（目前主要还是分析结论层）。  
2. **把“逐字匹配”升级为“业务语义判定”**（尤其 I/M/N）并形成稳定实现。  
3. **决定是否启用白名单/特例覆盖策略**（已有 8 行及 24 行冲突样本依据）。  
4. **补充 pytest 自动化**（建议离线 SQL dump 驱动，回归验证 I/M/N 与责任人口径）。  
5. 若目标是“可解释的接近100%”：需要与业务确认“Excel展示口径 vs DB实时口径”的优先级。  

---

## 6. 建议下一位AI的执行顺序

1. 先读：`document/7_待处理文件SQL对照表.md` 的第 4.3~4.7 节。  
2. 再读：`document/file2_i_conflict_samples.json`（看冲突归因）。  
3. 再读：`.tmp_file2_imn_deepdive.py` 与 `.tmp_file2_1818_full_validation.py`（复用规则与函数）。  
4. 在主程序新增“文件2规则层”：
   - I：代码/路径语义匹配
   - M：日期关系（按子场景分流）
   - N：空对空 + 容差
5. 增加 pytest 回归集：
   - 核心业务断言（P1/P2/P3/P4）
   - 冲突样本断言（24条）
   - 责任人断言（已完成接口100%映射）
6. 复测并更新 `document/7_待处理文件SQL对照表.md` 最终口径。

---

## 7. 接手时容易踩坑的点

1. 不要把 `A` 当程序接口号；程序链路口径应以 `R` 为主。  
2. `I/M/N` 不能只做字符串等值比较，否则会低估可对齐程度。  
3. 文件2责任人要用完成人口径，不要回退到设总列作为主列。  
4. `INTINTERFACEDOC` 同 `A+E` 可能非唯一，必须结合 `R`/桥表消歧。  
5. 离线库和模板导出时间不一致，日期字段存在天然漂移。  

---

## 8. 补充说明

- 本移交说明覆盖了当前轮次前后全部关键记忆点与落地产物。  
- 若接手方需要逐步还原上下文，请优先使用对话转录文件与本说明交叉核对。  

