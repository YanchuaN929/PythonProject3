# 新AI接手全面移交说明（截至当前会话）

## 0. 1分钟总览（先看这段）

- 本项目核心目标：把“6类待处理文件”的 Excel 处理逻辑逐步改造成可由 SQL 数据驱动，尤其解决文件2/4“责任人”链路。
- 已完成的最大改造：
  - **文件1已切到数据库只读模式**（含 GUI 状态提示、导出改造、连接诊断按钮、pytest 覆盖）。
  - `sql_explorer` / `sql_explorer_onefile` 的实机探索闭环流程已建立，且修复了 onefile 资源路径、datetime 序列化、阻塞态误报失败等问题。
  - 文件3/4/6业务列 SQL 映射分析已完成并沉淀文档。
- 当前最关键进展：`example/CIMS-sql` 已补入分发表数据（不再只是 schema），已能做分发链路分析。
- 当前未完成核心：文件2/4责任人链路的**逐行精确覆盖率最终版**（需要文件2/4源 Excel 在本机可读路径）。

---

## 1. 项目背景与主线任务

- 项目：`E:/program/PythonProject3`
- 主任务长期分两条：
  - 业务侧：6类文件的“业务字段口径”与 SQL 对齐（含责任人、时间、完成标记、科室等）。
  - 工程侧：本机开发 -> 实机连库运行 -> 回传结果 -> 本机分析 的双机协作闭环。
- 历史重点关注：
  - 文件2/4责任人不是简单单表字段，可能需要通过 `INT/SEND -> 分发表` 多表链路恢复。
  - 文件6存在双分支（`INTINTERFACEDOC` 分支 + `SENDRECEIVEDATA` 分支）。

---

## 2. 已完成代码改造（程序层）

## 2.1 文件1数据库驱动（核心已落地）

- 新增目录与模块：
  - `core/sql/__init__.py`
  - `core/sql/file1_db_source.py`
- 主要能力：
  - 虚拟源路径：`db://file1/{project_id}`
  - 使用已保存 `sql_explorer` profile 连接数据库（优先 `pymssql/pyodbc`）
  - 查询并构建文件1标准 DataFrame
  - 文件1导出收敛为4列：`接口号/接口日期/责任人/所属科室`
- 接入点：
  - `core/main.py`：`process_target_file` 支持虚拟源直接查库；`export_result_to_excel` 支持 DB 模式导出。
  - `base.py`：文件识别、缓存、Registry、指派、导出、UI 文案均已按“文件1 DB 只读模式”分支处理。

## 2.2 GUI与可运维性

- `base.py` 新增“测试数据库连接”按钮回调：`on_test_db_connection`
- 新增诊断报告输出逻辑：
  - 驱动模块检查（`pymssql` / `pyodbc`）
  - SQL Explorer profile 检查
  - SQL Server connect test
  - 文件1DB状态
  - Registry SQLite 健康检查
- 诊断文件路径在可写目录，便于实机回传排障。

## 2.3 打包与 onefile 运行修复

- `scripts/db_tools/sql_explorer/cli.py`：
  - 新增 `_resource_base()`、`_resolve_runtime_file_path()`，修复 onefile 模式样本资源路径。
  - 新增 `distribution-chain` 子命令（专用于文件2/4责任人链路）。
  - 优化阻塞态文案：`exit_code=3` 时显示“程序执行完成（存在阻塞，请查看报告）”。
- `scripts/db_tools/sql_explorer/distribution_chain_live.py`：
  - 增加 JSON 安全序列化 `_json_default`（处理 datetime / bytes）。

---

## 3. 已完成分析结论（业务映射）

## 3.1 文件2（1818）

- `R`（接口号存在性）对 `IDIACP1000.ITEM_NUMBER`：100%
- `R -> INTINTERFACEDOC` 链接：`15775/15776 = 99.9937%`
- 已完成样本责任人主口径建议：
  - 首选：`INTINTERFACEDOC.MODIFIED_BY_ID -> USER`
  - 回退链：`INT.MODIFIED_BY_ID -> IDI.DEPART_USER -> IDI.MODIFIED_BY_ID -> INT.CREATED_BY_ID`
  - 不建议把 `RESP_SHEZONG` 作为“完成人主口径”。

## 3.2 文件3/4/6（已形成专项文档）

- 详见：`document/9_待处理文件3_4_6业务列SQL对照分析.md`
- 核心结论：
  - 文件3主映射稳定可落地。
  - 文件4 `AF/AC/S` 稳定，`P/V` 明显是派生规则，不是单列直连。
  - 文件6必须双分支路由（INT分支 + SEND分支）。

## 3.3 文件2/4“责任人分发链路”先验证据（旧结论）

- 详见：`document/7_待处理文件SQL对照表.md` 的 4.6 节、`document/file2_4_distribution_chain_probe.json`
- 当时结论是“离线包缺分发表数据，无法闭环”。
- **该结论已过时一部分**：本轮确认 `example/CIMS-sql` 已补入分发表数据。

---

## 4. 本轮新增关键进展（你刚补的 CIMS-sql）

## 4.1 离线库现状确认（`example/CIMS-sql`）

已确认存在且有数据的关键表（含 insert 行数）：

- `DISTRIBUTERECORD`: 2,372,886
- `OBJECTREPLYLINK`: 48,767
- `FILETRANSMISSION`: 242,331
- `CRREPLY`: 25,296
- `DCRREPLY`: 0
- `FCRREPLY`: 302
- `NCRREPLY`: 4,152
- `TCRREPLY`: 0
- `TAREPLY`: 20,039
- 以及基础主表：`INTINTERFACEDOC`、`SENDRECEIVEDATA`、`USER`、`DEPARTMENT`、`TA` 等

补充说明：

- 本轮发现 `CIMS-sql` 根目录为空，但 `example/CIMS-sql` 数据完整，后续分析应统一以 `example/CIMS-sql` 为准。

## 4.2 本轮快速链路扫描（样本证据，非最终覆盖率）

方法：从 `INTINTERFACEDOC` 与 `SENDRECEIVEDATA` 抽取 1818 相关 500 个样本 ID，反查分发表是否出现这些 ID。

结果（仅作“有无链路证据”）：

- 文件2方向（INT样本）：
  - `DISTRIBUTERECORD` 命中 `7/500`（1.4%）
  - `OBJECTREPLYLINK` 命中 `0/500`
- 文件4方向（SEND样本）：
  - `FILETRANSMISSION` 命中 `36/500`（7.2%）
  - `CRREPLY` 命中 `4/500`（0.8%）
  - `TAREPLY` 命中 `2/500`（0.4%）
  - `TA` 命中 `2/500`（0.4%）
  - `DCRREPLY` / `TCRREPLY` 为空表

解释：

- 这说明“链路表现在可用了”，但不同链路覆盖差异很大，必须做下一步“逐行精确覆盖率”。
- 当前样本法不等于最终业务口径，不可直接作为验收结论。

---

## 5. 关键文档地图（新AI先读）

- 总对照表（含文件2/4分发链节）：`document/7_待处理文件SQL对照表.md`
- 文件3/4/6专项：`document/9_待处理文件3_4_6业务列SQL对照分析.md`
- 实机任务书（双机流程、命令、回拷标准）：`document/10_sql_explorer_onefile_实机探索任务书.md`
- 文件2全量验证：`document/file2_1818_full_validation_latest.json`
- 文件2/4分发链旧探针：`document/file2_4_distribution_chain_probe.json`

---

## 6. 未完成工作（必须继续）

## 6.1 文件2/4责任人链路最终定稿（最重要）

目标：输出“可上线口径”而非样本探针。

- 文件2：按 `(A,D) -> INT -> 分发表` 逐行算覆盖率
- 文件4：按 `E -> SEND -> 分发表` 逐行算覆盖率
- 输出必须包含：
  - 每条链路覆盖率
  - 候选责任人字段评分（非空率/可解析USER率/与Excel责任人匹配率）
  - 主字段 + 回退字段建议
  - 异常样本清单

## 6.2 需要的输入（当前缺口）

- 需要文件2/4 Excel 源文件可读路径（此前历史路径在 `example/`，但当前本机未检测到这两个样本）。
- 没有这两个 Excel 时，只能做结构/样本证据，不能做最终逐行覆盖率。

## 6.3 文档回写

- 完成后需回写：
  - `document/7_待处理文件SQL对照表.md`（更新 4.6 章节为“补数后最终结论”）
  - 另产出一份新 JSON/MD 结果，避免覆盖旧证据。

---

## 7. 给新AI的推荐执行步骤（可直接照做）

- 第一步：确认输入文件存在
  - 检查文件2/4 Excel 实际路径；若不存在，先向用户要路径。
- 第二步：跑离线精确探针
  - 优先使用已有脚本：`scripts/db_tools/sql_explorer/real_distribution_chain_probe.py`
  - 指向：
    - `--sql-dir E:/program/PythonProject3/example/CIMS-sql`
    - `--file2-excel <文件2路径>`
    - `--file4-excel <文件4路径>`
- 第三步：产出与验收
  - 输出 `real_distribution_chain_report.md/json + coverage csv + owner score csv`
  - 给出“可上线主口径 + 回退口径 + 风险告警”。

---

## 8. 已知风险与坑点

- 超大 SQL 文件（尤其 `DISTRIBUTERECORD.sql`）会导致脚本卡顿，避免整文件一次性读入字符串。
- `DCRREPLY`、`TCRREPLY` 当前为空，不能指望从这两表拿到主责任人证据。
- `OBJECTREPLYLINK` 的主价值更可能是对象号/文号关联，不一定直接承载责任人。
- onefile 下资源路径与本地调试路径不同，命令必须考虑 `_MEIPASS` 兼容（已在 `cli.py` 修复）。
- 工作区是脏树状态，且有历史改动未提交；新AI应避免误回滚非本次改动。

---

## 9. 当前代码与仓库状态提醒

- 当前分支存在多处未提交改动（含 `base.py`、`core/main.py`、`sql_explorer` 相关文件等）。
- 本次会话未新增持久代码改造（只做分析与结论），仅新增本移交文档。
- 若新AI要继续编码，务必先 `git status` 审核后再改，避免覆盖已有工作。

---

## 10. 对下一个AI的交付目标（验收标准）

- 能让用户看到一份“文件2/4责任人链路最终可执行口径”：
  - 文件2：主链 + 回退链 + 覆盖率
  - 文件4：主链 + 回退链 + 覆盖率
- 能明确哪些表/字段是“强证据”，哪些只是“辅助证据”。
- 能把结果回写到对照文档中，并保留结构化产物供后续自动化复算。

