# 待处理文件3/4/6业务列 SQL 对照分析（1818，CIMS-sql）

## 1. 分析范围与口径

- 目标：延续文件2的分析方法，只看 `document/2_模块功能说明.md` 第 70~73 行中的**业务列条件**。
- 数据源：
  - Excel样本：`example/外部接口ICM报表181820260128.xlsx`、`example/外部接口单报表181820260128.xlsx`、`example/收发文清单1818.xlsx`
  - SQL快照：`example/CIMS-sql/ICMACP1000.sql`、`example/CIMS-sql/INTINTERFACEDOC.sql`、`example/CIMS-sql/SENDRECEIVEDATA.sql`
- 统一口径：
  - 项目：`1818`
  - 日期窗口：按程序当前实现（本次按 `2026-01-28` 复算）
  - 日期偏移：1818 项目按 `adjust_date_for_project` 减 6 天
  - 版本：SQL优先 `IS_CURRENT`，并列按 `MODIFIED_ON` 取最新

## 2. 文件3（外部需打开接口）业务列

业务规则（模块说明）：
- `I列='B'` 且 `AL列` 组织前缀
- 组1：`M` 日期范围 + `T` 为空
- 组2：`L` 日期范围 + `Q` 为空
- 最终：`组1 ∪ 组2`

### 2.1 行级对齐覆盖

- Excel行数：`4677`
- 通过 `C(接口编码)` -> `ICMACP1000.ITEM_NUMBER` 对齐：`4677/4677`（100%）

### 2.2 业务列映射与一致率（按条件语义）

- `I`（发布方） -> `RELEASE_PARTY`：`1.000000`
- `I` -> `RECEIVE_PARTY`：`0.000000`（可排除）
- `AL`（主办所前缀） -> `RESP_DEPART` 前缀判断：`0.993586`
- `M`（日期范围） -> `FINAL_FORECAST_DATE`：`0.995938`
- `L`（日期范围） -> `PRE_FORECAST_DATE`：`0.998931`
- `T`（为空） -> `FINAL_OPEN_DATE` 为空：`0.965362`
- `Q`（为空） -> `PRE_OPEN_DATE` 为空：`0.998503`

### 2.3 结论

- 文件3的业务列主映射可明确为：
  - `I -> RELEASE_PARTY`
  - `AL -> RESP_DEPART`
  - `M/L -> FINAL_FORECAST_DATE / PRE_FORECAST_DATE`
  - `T/Q -> FINAL_OPEN_DATE / PRE_OPEN_DATE`
- 在本日期口径下，文件3最终条件命中为 0（Excel 与 SQL 都是 0），因此“最终逻辑 100%”不具判别力，需看上述子条件一致率。

## 3. 文件4（外部需回复接口）业务列

业务规则（模块说明）：
- `AF` 组织前缀
- `P='B'` 或 (`P` 空 且 `AC='B'`)
- `S` 日期范围
- `V` 为空

### 3.1 行级对齐覆盖

- Excel行数：`17830`
- 通过 `接口编码(列23)` -> `ICMACP1000.ITEM_NUMBER` 对齐：`17830/17830`（100%）

### 3.2 业务列映射与一致率（按条件语义）

- `AF` 前缀 -> `RESP_DEPART` 前缀：`0.993045`
- `AC`（接收方） -> `RECEIVE_PARTY`：`1.000000`
- `P`（处理方）直接映射：
  - 对 `RELEASE_PARTY`：`0.625351`
  - 对 `RECEIVE_PARTY`：`0.321537`
- `P` 条件映射：
  - 用 `RELEASE_PARTY` 重建 `P='B' or (P空且AC='B')`：`0.690017`
  - 用 `RECEIVE_PARTY` 重建同条件：`0.418845`
- `S` 日期范围：
  - 对 `PRE_CLOSE_DATE`：`0.955861`（最佳）
  - 对 `FINAL_CLOSE_DATE`：`0.921144`
  - 对 `CLOSE_DATE`：`0.939204`
- `V` 为空：
  - 对 `FINAL_CLOSE_DATE` 为空：`0.709815`
  - 对 `CLOSE_DATE` 为空：`0.624734`

### 3.3 结论

- 文件4稳定映射：
  - `AF -> RESP_DEPART`
  - `AC -> RECEIVE_PARTY`
  - `S -> PRE_CLOSE_DATE`（优先）
- `P`（处理方）不是纯一对一 SQL 原子列，当前更像报表派生字段；若要 SQL 端复刻，需补“处理方派生规则”。
- `V` 也存在明显派生痕迹，直接用 `FINAL_CLOSE_DATE/CLOSE_DATE` 的空值只能部分解释。

## 4. 文件6（收发文函）业务列

业务规则（模块说明）：
- `V` 包含 `河北分公司.建筑结构所`
- `I` 非空且日期 `<=14天`（管理员跳过日期）
- `M in ('尚未回复','超期未回复')`

### 4.1 键值形态与主表分流

`E(收发文编号)`存在两类键：
- `1818-5-...` 形态：`379` 行（接口单口径）
- 字母发文号形态（如 `EFZX-400...`）：`1248` 行（文函口径）

覆盖率（已实测）：
- `INTINTERFACEDOC.ITEM_NUMBER` 精确命中：`325`
- `INTINTERFACEDOC` 去尾号补偿命中：`366`
- `SENDRECEIVEDATA.(CORRESP_LETTER_REC_NO/LETTER_SEND_NO)` 命中：`1304`
- 两路并集命中：`1629/1696`（`96.05%`）

### 4.2 INT 分支（366 行）业务列一致率

SQL侧重建（INT分支）：
- `V` 条件：`PROPOSED_DEPT/RECEIVE_DEPT` 包含 `25C1/25C2/25C3` 或“结构”字样
- `I` 条件：`REPLY_DEADLINE`
- `M` 条件：`ANSWER_DATE` 为空

一致率：
- `P1(V组织)`：`0.849727`
- `I非空`：`0.989071`
- `I<=14天`：`0.989071`
- `M状态`：`0.844262`

### 4.3 SEND 分支（规则结论）

针对 `EFZX-.../ECZB-.../EDES-...` 等文函号，SQL证据显示主要落在 `SENDRECEIVEDATA`：
- 键：`CORRESP_LETTER_REC_NO` / `LETTER_SEND_NO`
- 日期：`REPLY_DEADLINE`
- 状态：`ANSWER_DATE` + `IS_ANSWERED`
- 组织：`AUTHOR_UNIT` / `RECEIVE_UNIT`（含 `ECZS`、结构所字样、编码字典）

结论：文件6应采用**双分支路由**，不能只绑定单表。

## 5. 最终建议（可直接用于SQL改造）

1. 文件3：按 `ICMACP1000` 直接落地，字段映射稳定。
2. 文件4：`AF/AC/S` 可直接落地；`P/V` 需要补“报表派生规则”后再追求高一致。
3. 文件6：先按 `E列键形态` 分流：
   - `1818-5-...` -> `INTINTERFACEDOC`
   - 文函号 -> `SENDRECEIVEDATA`
4. 文件6 的 `V` 与 `M` 建议引入一层“组织编码/状态转换字典”，否则难以达到与 Excel 展示口径一致。

## 6. 补充探索（2026-02-28）

### 6.1 文件6：先验分流进一步验证

基于 `INTINTERFACEDOC`（当前版 + 去尾号）补充统计：
- 总行数：`1696`
- `INT` 精确命中：`325`
- `INT` 去尾号命中：`366`
- `INT` 路径未命中：`1330`

未命中类型分布：
- `letter_key`：`1248`
- `int_key`：`21`
- `other_key`：`57`
- `empty_key`：`4`

结论：
- `1248` 条字母发文号样式（`EDMB/ECZB/EFCS/...`）进一步验证必须走 `SENDRECEIVEDATA` 分支。
- `INT` 未命中的 `21` 条 `1818-5-...` 样式，在 `INTINTERFACEDOC` 与 `ICMACP1000` 中均未检出（含去尾号），属于“源侧缺失/口径不一致”候选，建议单独建异常字典。
- `other_key` 里以 `1818BW...` 为主，建议归入“收发文编号特殊编码族”，后续在 `SENDRECEIVEDATA` 增加专项映射策略。
- 进一步全量核验（`SENDRECEIVEDATA.CORRESP_LETTER_REC_NO / LETTER_SEND_NO`）：
  - `other_key`（`56` 个唯一键）在归一化后 `56/56` 可匹配（示例：`1818BW22C520260001` ↔ `1818-BW-22C5-2026-0001`）。
  - `int_key`（`21` 个唯一键，`1818-5-...`）在 SEND 两列 `0/21` 未检出，说明其主要不是文函号分支数据。

### 6.2 文件4：`P/V` 派生规则补充

在原结论（`AF/AC/S` 稳定）基础上，对 `P/V` 做了进一步规则试探：

- `P` 条件（`P='B' or (P空且AC='B')`）：
  - 仅用 `RELEASE_PARTY`：`0.690017`
  - 仅用 `RECEIVE_PARTY`：`0.418845`
  - SQL-only 简化候选（两方同时 `B` 视为命中）：`0.747112`（高于单列映射）

- `V` 为空条件：
  - `FINAL_CLOSE_DATE` 为空：`0.709815`
  - `CLOSE_DATE` 为空：`0.624734`
  - 组合候选（`FINAL_CLOSE_DATE` 空且 `CLOSE_DATE` 非空）：`0.716040`（小幅提升）

结论：
- `P/V` 确认是“派生字段”而非单列映射，建议进入“规则层”实现，不建议在对照表中标注为固定一列直连。
- `P` 可先采用“保守 SQL-only 规则（0.747）”上线，再用项目字典做二次提升。

