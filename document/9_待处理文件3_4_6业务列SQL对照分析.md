# 待处理文件3/4/6业务列 SQL 对照分析（1818，CIMS-sql）

## 1. 分析范围与口径

- 目标：延续文件2的分析方法，只看 `document/2_模块功能说明.md` 第 70~73 行中的**业务列条件**。
- 数据源：
  - Excel样本：`example/外部接口ICM报表181820260128.xlsx`、`example/外部接口单报表181820260128.xlsx`、`example/收发文清单1818.xlsx`
  - SQL快照：`example/CIMS-sql/ICMACP1000.sql`、`example/CIMS-sql/INTINTERFACEDOC.sql`、`example/CIMS-sql/SENDRECEIVEDATA.sql`
- 统一口径：
  - 项目：`1818`
  - 日期窗口：按程序当前实现（本次按 `2026-01-28` 复算）
  - 日期偏移：1818 项目按 `adjust_date_for_project` 减 6 天
  - 版本：SQL优先 `IS_CURRENT`，并列按 `MODIFIED_ON` 取最新

## 2. 文件3（外部需打开接口）业务列

业务规则（模块说明）：
- `I列='B'` 且 `AL列` 组织前缀
- 组1：`M` 日期范围 + `T` 为空
- 组2：`L` 日期范围 + `Q` 为空
- 最终：`组1 ∪ 组2`

### 2.1 行级对齐覆盖

- Excel行数：`4677`
- 通过 `C(接口编码)` -> `ICMACP1000.ITEM_NUMBER` 对齐：`4677/4677`（100%）

### 2.2 业务列映射与一致率（按条件语义）

- `I`（发布方） -> `RELEASE_PARTY`：`1.000000`
- `I` -> `RECEIVE_PARTY`：`0.000000`（可排除）
- `AL`（主办所前缀） -> `RESP_DEPART` 前缀判断：`0.993586`
- `M`（日期范围） -> `FINAL_FORECAST_DATE`：`0.995938`
- `L`（日期范围） -> `PRE_FORECAST_DATE`：`0.998931`
- `T`（为空） -> `FINAL_OPEN_DATE` 为空：`0.965362`
- `Q`（为空） -> `PRE_OPEN_DATE` 为空：`0.998503`

### 2.3 结论

- 文件3的业务列主映射可明确为：
  - `I -> RELEASE_PARTY`
  - `AL -> RESP_DEPART`
  - `M/L -> FINAL_FORECAST_DATE / PRE_FORECAST_DATE`
  - `T/Q -> FINAL_OPEN_DATE / PRE_OPEN_DATE`
- 在本日期口径下，文件3最终条件命中为 0（Excel 与 SQL 都是 0），因此“最终逻辑 100%”不具判别力，需看上述子条件一致率。

## 3. 文件4（外部需回复接口）业务列

业务规则（模块说明）：
- `AF` 组织前缀
- `P='B'` 或 (`P` 空 且 `AC='B'`)
- `S` 日期范围
- `V` 为空

### 3.1 行级对齐覆盖

- Excel行数：`17830`
- 通过 `接口编码(列23)` -> `ICMACP1000.ITEM_NUMBER` 对齐：`17830/17830`（100%）

### 3.2 业务列映射与一致率（按条件语义）

- `AF` 前缀 -> `RESP_DEPART` 前缀：`0.993045`
- `AC`（接收方） -> `RECEIVE_PARTY`：`1.000000`
- `P`（处理方）直接映射：
  - 对 `RELEASE_PARTY`：`0.625351`
  - 对 `RECEIVE_PARTY`：`0.321537`
- `P` 条件映射：
  - 用 `RELEASE_PARTY` 重建 `P='B' or (P空且AC='B')`：`0.690017`
  - 用 `RECEIVE_PARTY` 重建同条件：`0.418845`
- `S` 日期范围：
  - 对 `PRE_CLOSE_DATE`：`0.955861`（最佳）
  - 对 `FINAL_CLOSE_DATE`：`0.921144`
  - 对 `CLOSE_DATE`：`0.939204`
- `V` 为空：
  - 对 `FINAL_CLOSE_DATE` 为空：`0.709815`
  - 对 `CLOSE_DATE` 为空：`0.624734`

### 3.3 结论

- 文件4稳定映射：
  - `AF -> RESP_DEPART`
  - `AC -> RECEIVE_PARTY`
  - `S -> PRE_CLOSE_DATE`（优先）
- `P`（处理方）不是纯一对一 SQL 原子列，当前更像报表派生字段；若要 SQL 端复刻，需补“处理方派生规则”。
- `V` 也存在明显派生痕迹，直接用 `FINAL_CLOSE_DATE/CLOSE_DATE` 的空值只能部分解释。

## 4. 文件6（收发文函）业务列

说明：本节原有结论基于 1818 单项目样本。自 `2026-03-12` 起，文件6统一以 7 项目整合样本及已合并后的 `document/13_CIMS-SQL-3.5_文件6_SQL深挖复核_20260311.md` 为准。

### 4.1 最新统一结论（7项目）

- 总样本：`4178`
- 当前总路由：
  - `INT = 1056`
  - `SEND = 2945`
  - `unresolved = 177`
  - 总覆盖 `4001 / 4178 = 95.7635%`
- 当前稳定分支规则：
  - `E` 含 `-ZL-` -> `INTINTERFACEDOC`
  - 其余文函号与 `BW` 特殊号 -> `SENDRECEIVEDATA`

### 4.2 字段结论

#### INT 分支

- `E -> ITEM_NUMBER = 100.0000%`
- `I -> REPLY_DEADLINE = 100.0000%`
- `J -> ANSWER_DATE = 81.9129%`
- `M` 派生 = `92.7083%`
- `AC -> REV = 60.8902%`

#### SEND 分支

- `E -> CORRESP_LETTER_REC_NO / LETTER_SEND_NO = 100.0000%`
- `I -> REPLY_DEADLINE = 58.6078%`
- `J -> ANSWER_DATE = 94.3633%`
- `M` 派生：
  - `2026-03-04` 复算 = `75.1783%`
  - `2026-03-11` 复算 = `71.3413%`
- `AC` 当前仍无稳定 SQL 字段

### 4.3 Word 版修正后的关键变化

1. `X` 不再按 `V` 所属所判断
2. `X` 当前统一理解为“分发全过程办理人并集”
3. `V/W` 当前统一理解为这些办理人对应的单位/科室并集
4. Excel 与 SQL 多值只要任一交集命中即算命中
5. “出现回文日期即完成”与 `J` 列逻辑一致

### 4.4 当前未闭环项

- `SEND` 分支稳定分发表对象桥
- `X/V/W` 的全量 SQL 恢复
- `SEND` 分支 `AC`
- `H` 当前按最新要求暂缓

## 5. 当前建议

1. 文件3口径保持不变
2. 文件4继续按 `SENDRECEIVEDATA + IICS/IITF + 分发表` 理解
3. 文件6后续若继续深挖，优先顺序改为：
   - `FILETRANSMISSION / TA / CR / NCR ... -> DISTRIBUTERECORD` 对象桥
   - 再恢复 `X`
   - 最后处理 `V/W/AC/H`
