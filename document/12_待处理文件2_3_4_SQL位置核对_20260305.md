# 待处理文件2/3/4 SQL位置核对（2026-03-05）

## 1. 本轮输入与核对范围

- 前端口径说明：`document/接口信息的对应.pdf`（4页）
- SQL快照目录：`example/CIMS-sql`
- Excel样本：
  - `example/内部接口信息单报表181820260128.xlsx`（文件2）
  - `example/外部接口ICM报表181820260128.xlsx`（文件3）
  - `example/外部接口单报表181820260128.xlsx`（文件4）
- 目标：逐一核对文件2~4在 SQL 中的数据位置（表/字段/链路），为“去 Excel 依赖”改造做落点确认。

---

## 2. 文件2（内部需回复接口）核对结果

## 2.1 主键/基础字段位置

- `A(信息单编号)` -> `INTINTERFACEDOC.ITEM_NUMBER`（col_idx=`27`）
- `D(对方文号)` -> `INTINTERFACEDOC.REF_ITEM_NUMBER`（col_idx=`37`）
- `R(IDI编号)` -> `IDIACP1000.ITEM_NUMBER`（col_idx=`27`）
  - 关联链：`INTINTERFACEDOC.id` -> `INTINTERFACEDOCIDIACP1000.SOURCE_ID`（col_idx=`22`） -> `INTINTERFACEDOCIDIACP1000.RELATED_ID`（col_idx=`26`） -> `IDIACP1000.id`

## 2.2 业务列位置（按模块说明第69行）

- `I(收文部门)`：
  - 主：`INTINTERFACEDOC.PROPOSED_DEPT`（`38`）
  - 补：`INTINTERFACEDOC.RECEIVE_DEPT`（`39`）
- `M(回文期限)`：
  - 主：`INTINTERFACEDOC.START_DATE`（`47`）+ 项目规则（1818项目存在偏移/天数规则）
  - 补：`INTINTERFACEDOC.REPLY_DEADLINE`（`57`）
- `N(回文日期)`：
  - 主：`INTINTERFACEDOC.ANSWER_DATE`（`58`）

## 2.3 责任人位置（重点）

- PDF口径：责任人来自“内部接口信息单（传递）-> 分发信息 -> 办理人链（逐级分发）”，不是单表固定列。
- SQL链路主口径：
  - `INTINTERFACEDOC.id` -> `DISTRIBUTERECORD.SOURCE_OBJECT_ID`（`32`）
  - 候选责任人字段：`DISTRIBUTERECORD.OPERATOR`（`29`）/ `SENDER`（`25`）
- 回退口径（当分发链数据缺失）：
  - `INTINTERFACEDOC.MODIFIED_BY_ID`（`9`）
  - `INTINTERFACEDOC.CREATED_BY_ID`（`5`）
  - 再经 `USER/DEPARTMENT` 解析姓名与科室

## 2.4 本轮实测（1818样本）

- 行数：`15776`
- `A -> INT.ITEM_NUMBER`：`15775/15776 = 99.9937%`
- `D -> INT.REF_ITEM_NUMBER`：`7238/15776 = 45.8798%`（符合其“对方文号”辅助键属性）
- `(A,D) -> INT行`：`15775/15776 = 99.9937%`
- `R -> (桥表) -> IDI.ITEM_NUMBER`：`15775/15776 = 99.9937%`
- 分发表覆盖（离线探测）：
  - `INT.id -> DISTRIBUTERECORD.SOURCE_OBJECT_ID`：`215/7063 = 3.0440%`
  - 说明：当前离线快照在 file2 责任人主链上覆盖不足，需继续以“分发表补数/实机联调”闭环。

---

## 3. 文件3（外部需打开接口）核对结果

## 3.1 主键与业务列位置

- 主表：`ICMACP1000`
- 主键：`C(接口编码)` -> `ICMACP1000.ITEM_NUMBER`（`27`）
- 业务列落点：
  - `I(发布方)` -> `RELEASE_PARTY`（`35`）
  - `AL(主办所)` -> `RESP_DEPART`（`40`）
  - `L(初版预报日期)` -> `PRE_FORECAST_DATE`（`44`）
  - `M(终版预报日期)` -> `FINAL_FORECAST_DATE`（`45`）
  - `Q(初版打开日期)` -> `PRE_OPEN_DATE`（`57`）
  - `T(终版打开日期)` -> `FINAL_OPEN_DATE`（`60`）

## 3.2 本轮实测（1818样本）

- 行数：`4677`
- `C -> ICM.ITEM_NUMBER`：`4677/4677 = 100%`
- `I == RELEASE_PARTY`：`4677/4677 = 100%`

结论：文件3可直接 SQL 驱动，主表与核心字段位置明确且稳定。

---

## 4. 文件4（外部需回复接口）核对结果

## 4.1 业务列主链（ICM）

- 主表：`ICMACP1000`
- 主键：`列23(接口编码)` -> `ICMACP1000.ITEM_NUMBER`（`27`）
- 业务列落点：
  - `AF(主办所)` -> `RESP_DEPART`（`40`）
  - `AC(接收方)` -> `RECEIVE_PARTY`（`36`）
  - `S(回文期限筛选)` -> `PRE_CLOSE_DATE`（`58`）优先
  - `V(回文日期)`：非稳定单列直连，属于派生口径（`FINAL_CLOSE_DATE`/`CLOSE_DATE` 仅能部分解释）
  - `P(处理方)`：亦为派生口径，不建议标注为单列直连

## 4.2 责任人链（SEND + 分发表）

- PDF口径：进入接口单号后在“分发信息”判定责任人，逻辑与文件2类似（办理人链）。
- SQL责任人主链：
  - `E(接口单号)` -> `SENDRECEIVEDATA.LETTER_SEND_NO`（`35`）/ `CORRESP_LETTER_REC_NO`（`27`）
  - 再由 `SENDRECEIVEDATA.id` -> `DISTRIBUTERECORD.SOURCE_OBJECT_ID`（`32`）取 `OPERATOR/SENDER`
- 回退口径：
  - `SENDRECEIVEDATA.CREATED_BY_ID`（`4`）
  - `SENDRECEIVEDATA.MODIFIED_BY_ID`（`8`）
  - 再经 `USER/DEPARTMENT` 解析

## 4.3 本轮实测（1818样本）

- 行数：`17830`
- `列23(接口编码) -> ICM.ITEM_NUMBER`：`17830/17830 = 100%`
- `AC == RECEIVE_PARTY`：`17830/17830 = 100%`
- `E -> SEND.LETTER_SEND_NO`（唯一匹配）：`16037/17822 = 89.9843%`
- `E -> SEND.CORRESP_LETTER_REC_NO`（唯一匹配）：`7706/17822 = 43.2387%`
- `SEND.id -> DISTRIBUTERECORD.SOURCE_OBJECT_ID`：`0/16272 = 0%`（离线快照数据缺口）

---

## 5. 当前阻塞与结论

## 5.1 阻塞点

- 分发表相关链路在当前离线快照对 file2/file4 覆盖明显不足（尤其 file4）。
- 关联回复表存在缺口：
  - `DCRREPLY` 空表
  - `TCRREPLY` 空表
  - `MEMORANDUM` / `TELEFAX` 仅schema无数据

## 5.2 可直接落地部分

- 文件3：可直接 SQL 化（主表 `ICMACP1000`）。
- 文件4：`AF/AC/S` 可直接 SQL 化；`P/V` 需规则层派生。
- 文件2：`A/D/R` 主键链路可落地；责任人主链需依赖分发表补齐，当前应保留回退口径。

## 5.3 改造建议（面向“去Excel依赖”）

1. 先实现 file3 全 SQL 驱动（低风险闭环）。
2. file4 分两层：
   - 第一层：`AF/AC/S` 直连 SQL；
   - 第二层：`P/V + 责任人` 规则层（含回退口径）。
3. file2 先落地 `A/D/R/I/M/N`，责任人采用“分发表优先 + INT回退”。
4. 等你提供下一批实机备份后，优先补 file2/file4 分发表链路覆盖，再收敛为最终主字段。

---

## 6. 本轮产物路径

- 分发链离线探测：
  - `sql_explorer_output/real_distribution_chain_offline_20260305_163640/real_distribution_chain_report.json`
  - `sql_explorer_output/real_distribution_chain_offline_20260305_163640/real_distribution_chain_report.md`
  - `sql_explorer_output/real_distribution_chain_offline_20260305_163640/table_rowcount_and_coverage.csv`
  - `sql_explorer_output/real_distribution_chain_offline_20260305_163640/owner_candidate_scores.csv`
